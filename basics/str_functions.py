# This function returns the length of the strings.
name="jerry"
print(len(name))
# Capitalizes the string
print(name.capitalize())
# Checks if the string ends with rry or not
print(name.endswith("rry"))
# Checks if the string starts with jer or not
print(name.startswith("jr"))
